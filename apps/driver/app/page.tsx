import { FoundationShell } from '@atlas/ui';
export default function Page() {
  return (
    <FoundationShell title="Driver access is provisioned">
      <p>
        This Phase 1 shell validates the build and identity boundary. Routes and offline operations
        are not part of Foundation.
      </p>
    </FoundationShell>
  );
}
